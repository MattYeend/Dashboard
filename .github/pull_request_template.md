## Summary

Closes #

## Checklist

- [ ] Tests added or updated (Pest)
- [ ] Policy and permissions updated (`RolePermissionSeeder`)
- [ ] State changes are written to the audit log
- [ ] New tables have `organisation_id` (or are listed as central)
- [ ] No secrets, tokens or personal data in logs or fixtures
- [ ] `composer run lint:check`, `composer run analyse`, `composer run test` pass
- [ ] `npm run lint:check`, `npm run types:check`, `npm run build` pass
- [ ] README or docs updated if behaviour changed
